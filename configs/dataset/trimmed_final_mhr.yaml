# @package _global_
# trimmed_final.mp4, MHR-native track (pipelines/texture_appearance/threedgs_avatar_mhr/), reads
# the output of prepare_mhr_keyframe_track.py directly.

dataset_name: trimmed_final_mhr

dataset:
  name: mhr_native
  root_dir: "/mnt/ssd2/tdgs_avatar_runs/mhr_native_trimmed_final"
  mhr_model_path: "/mnt/ssd2/sam4d_body_checkpoints/sam-3d-body-dinov3/assets/mhr_model.pt"
  mhr_device: cuda

  padding: 0.1
  resolution: -1
  white_background: false
  data_device: cuda
  eval: true
